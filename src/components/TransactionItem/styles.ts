// External libraries
import styled from 'styled-components/native';

export const YBTransactionItem = styled.View`
  width: 100%;
  height: 54px;

  background-color: ${({ theme }) => theme.colors.item};
`;
